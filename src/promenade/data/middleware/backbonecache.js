define(['backbone'],
       function (Backbone) {

  var BackboneCache = function (data) {
    //var _cache = {};

    //function cache (result) {
      //cache[result.id] = {
        //timer: window.setTimeout(function () {

        //}, ttl)
      //};
    //}

    //return function (transaction) {

      //transaction.complete(function (context) {
        //return Backbone.sync.call(
            //Backbone, context.get('method'), context.get('model'), context.get('ajaxOptions'));
      //});
    //};
  };

  return BackboneCache;
});
